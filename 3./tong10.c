#include<stdio.h>

int main() {    
    int sum;
    int count;
    for(count = 1 ; count <= 10;count++){
    	sum = sum + count;
	}
	printf("tong tu mot den muoi = %d ", sum);
    return 0;    	 
}




#bao  gồm <stdio.h> 

int chính(){
  
int tong = 0 (int tong = 0);

for (int dem = 1; dem <= 10; dem++)
tong = tông + dem;
  {
    printf("tong la:%d ", tông);
  }
 người getchar();
  trả lại 0;
}
